<Window x:Class="Activer.LogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Activity Log"
        Width="500"
        Height="300"
        ResizeMode="NoResize"
        WindowStyle="None"
        Background="White">

    <Window.Resources>
        <Style x:Key="TitleBarIconButton" TargetType="Button">
            <Setter Property="Width" Value="36"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0,0,8,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="#666666"/>
            <Setter Property="HorizontalContentAlignment" Value="Right"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="HitArea"
                                Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="#B22222"/>
                                <Setter TargetName="HitArea" Property="Background" Value="Transparent"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="HitArea" Property="Background" Value="Transparent"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Height" Value="28"/>
            <Setter Property="MinWidth" Value="72"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#555555"/>
            <Setter Property="BorderBrush" Value="#C9CDD3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#F7F8FA"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#A8AFB8"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#EFF1F4"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#9CA3AD"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#999999"/>
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#F1F1F1"/>
                                <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#D6D6D6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="24"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Height="24"
              VerticalAlignment="Top"
              Background="Transparent"
              MouseLeftButtonDown="TopBar_MouseLeftButtonDown">
            <TextBlock Text="Activity Log"
                       HorizontalAlignment="Left"
                       VerticalAlignment="Center"
                       Margin="12,0,0,0"
                       FontWeight="Normal"
                       FontSize="12"
                       Foreground="#7A7A7A"/>

            <Button Style="{StaticResource TitleBarIconButton}"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Stretch"
                    Click="CloseButton_Click">
                <Path Data="M1,1 L8,8 M8,1 L1,8"
                      Width="9"
                      Height="9"
                      Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                      StrokeThickness="1.7"
                      Stretch="Fill"
                      StrokeStartLineCap="Round"
                      StrokeEndLineCap="Round"/>
            </Button>
        </Grid>

        <Grid Grid.Row="1" Margin="10,10,10,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0"
                        Orientation="Horizontal"
                        VerticalAlignment="Center"
                        Margin="0,0,8,0">
                <TextBlock x:Name="RunTimeTextBlock"
                           FontSize="14"
                           FontWeight="Bold"
                           Foreground="#333333"
                           Text="{Binding RunTimeText}"
                           VerticalAlignment="Center"/>

                <TextBlock x:Name="EndTimeTextBlock"
                           FontSize="14"
                           FontWeight="Bold"
                           Foreground="#333333"
                           Margin="12,0,0,0"
                           Text="{Binding EndTimeText}"
                           VerticalAlignment="Center"/>
            </StackPanel>

            <Button x:Name="ClearButton"
                    Grid.Column="1"
                    Content="Clear"
                    Style="{StaticResource SecondaryButton}"
                    Command="{Binding ClearLogCommand}"/>
        </Grid>

        <Border Grid.Row="2"
                Margin="10,0,10,10"
                BorderBrush="#D8D8DC"
                BorderThickness="1"
                CornerRadius="8">
            <ListBox x:Name="LogListBox"
                     Background="Transparent"
                     BorderThickness="0"
                     ItemsSource="{Binding Entries}"
                     FontFamily="Consolas"
                     FontSize="13"
                     Padding="6"
                     HorizontalContentAlignment="Stretch"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     ScrollViewer.HorizontalScrollBarVisibility="Auto">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" TextWrapping="Wrap"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>
    </Grid>
</Window>
